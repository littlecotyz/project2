[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackage"
version = "0.0.0"
description = "Scaffolded minimal package"
authors = [ { name = "Your Name" } ]
readme = "README.md"
license = {text = "MIT"}
dependencies = []
